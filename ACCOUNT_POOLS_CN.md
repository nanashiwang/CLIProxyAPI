# 账号分组与 Key 授权

管理面板新增「账号分组」入口。分组名称可以自定义，Key 可选择一个分组、多个分组或全部分组；实际绑定使用固定分组 ID，改名不影响权限。

## 使用步骤

1. 在配置面板添加客户端 API Key。
2. 打开「账号分组」，新建分组，并通过「分配账号」批量移入账号。
3. 在同页的「Key 调用范围」中选择指定分组或全部分组。
4. 勾选「启用账号分组权限」，点击「保存并应用」。

更改在保存后生效。未保存离开页面会提示；并发编辑冲突时会拒绝覆盖，可重新加载后再编辑。

## 范围规则

- 未分组的账号归入默认分组；没有配置专门规则的 Key 默认仅使用默认分组。
- 第一版一个凭据只能归入一个组。分组可混合包含不同提供商的凭据，执行时按模型和提供商筛选。
- 指定多个组时，使用所选组的账号并集。指定组列表为空，表示不能使用任何账号。
- 全部分组包括当前及未来新增的所有启用组，也会使用名称为“专用”的组。名称本身不代表独享权限。
- 停用分组后，其成员不再可被调度，包括全组 Key。
- 删除分组前先移出账号并解除 Key 的指定绑定。删除分组不会删除凭据文件。
- 调整权限后，新请求、下一次重试和 WebSocket 的下一轮请求重新检查权限。已经开始的请求可完成。
- 使用 API Key 管理接口单独替换 Key 时保留其分组规则；存在分组规则的多个 Key 同时替换会被拒绝，需逐个替换。直接编辑 YAML 时需同步维护 Key 指纹规则。
- 重命名或重新创建凭据文件、更换上游密钥可能产生新的凭据 ID，应检查其分组。重复导入同一上游账号不等于获得独立的上游额度。

## 支持范围

支持常用 HTTP API、SSE、Codex Responses WebSocket、组内重试、模型别名和会话绑定。模型列表按 Key 的分组权限过滤。

Home 调度暂不能与分组同时启用。分组启用期间，Realtime/Live、旧版 WebSocket 中继和绕过内置账号调度的独立插件执行器明确返回不可用；普通调度插件仍需通过候选范围和返回结果校验。

本功能使用已有配置持久化和热更新，不引入 Redis，不复制凭据 Token 或统计历史。新增统计记录携带 `client_key_id`（Key 指纹）和实际 `pool_id`；历史记录保持未归组状态。统计字段不参与权限判断。

## 管理 API

- `GET /v0/management/account-pools`：分组配置、Key 掩码与指纹、凭据列表、版本号。
- `PUT /v0/management/account-pools`：提交 `{ "config": ..., "revision": "GET 返回的版本号" }`。
- 版本过期返回 409；重复归组、无效分组引用等返回 400；保存失败不会切换运行中的权限。

配置字段见 `config.example.yaml` 中的 `account-pools` 示例。Key 规则通过客户端 Key 的 SHA-256 指纹关联；明文 Key 不出现在分组管理响应或路由元数据中。
