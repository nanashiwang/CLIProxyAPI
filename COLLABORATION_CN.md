# 协作与发布

`main` 是个人仓库的共同正式主线。个人仓库 PR 不再自动改投 dev，上游专用的 translator 路径禁改规则不在个人仓库运行；转换层修改仍需测试验证。功能开发从最新 `personal/main` 创建短期分支，完成后通过 PR 合入 main。不要强制推送覆盖伙伴的提交。

- 功能分支推送和 PR 运行检查、测试、构建及管理 API 冒烟检查，不创建正式 Release。检查使用提交内的模型目录，不在测试前下载其他版本覆盖源码；模型目录更新应单独提交验证。
- main 推送通过验证后自动创建个人版本标签。`[skip release]` 仍可用于无需发布的提交。
- 手动发布个人版本也必须使用属于 main 历史的个人版本标签；不要从未合并的功能分支发布 Latest。
- 发布工作流串行执行；旧版本重新构建时不会把已发布的新版本替换成 Latest。
- 管理面板使用相同规则：只有 main 的已验证代码能创建正式版本。
- 服务器一键更新读取个人仓库的最新 Release；更新脚本自身从 main 获取。发布代码不代表已经更新线上服务。

本次将 `feat/poo-gateway` 与朋友在 main 的改动合并，保留两边提交历史；已有分组、统计、模型兼容和租约行为不在本次重做。租约简化另开任务。

构建后使用 `.github/scripts/smoke-management-api.py <binary>` 启动隔离实例并验证 `/v0/management/account-pools`、`auth-files` 和 `usage`，避免仅通过编译但漏掉面板接口。脚本只使用临时凭据与临时目录。
